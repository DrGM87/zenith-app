# Zenith — Python sidecar dependencies
# Single install: pip install -r scripts/requirements.txt
#
# Script coverage:
#   process_files.py  · pdf_parser.py    · RAG_engine.py      · research_engine.py
#   scihub.py         · shazam_recognize.py · clinical_trials.py · fda_drug_label.py
#   ai_summarizer.py  · duplicate_finder.py · zenith_api.py

# ══════════════════════════════════════════════════════
# CORE — required by multiple scripts
# ══════════════════════════════════════════════════════

requests>=2.31.0          # HTTP client — clinical_trials, fda_drug_label, scihub, shazam, research_engine
urllib3>=1.26.0           # HTTP internals — scihub, research_engine (SSL warning suppression)
beautifulsoup4>=4.12.0    # HTML parsing — scihub.py
Pillow>=10.0.0            # Image processing — process_files.py, pdf_parser.py
numpy>=1.24.0             # Numeric arrays — shazam_recognize.py, RAG_engine.py
packaging>=23.0           # Version comparison — pdf_parser.py (_detect_available_libs)

# ══════════════════════════════════════════════════════
# PDF PROCESSING — pdf_parser.py
# ══════════════════════════════════════════════════════

pdfplumber>=0.10.0        # PDF text + table extraction (Tier 2)
pymupdf>=1.24.0           # Fast layout-aware extraction + PDF→PNG raster (Tier 3)
pypdf>=3.0.0              # Pure-Python extraction, last resort (Tier 4)
pikepdf>=8.0.0            # PDF compression (lossless, faster than PyMuPDF for that)
reportlab>=4.0.0          # PDF generation — research export, OCR→searchable PDF

# ══════════════════════════════════════════════════════
# RAG ENGINE — RAG_engine.py
# ══════════════════════════════════════════════════════

chromadb>=0.4.0           # Local vector store
sentence-transformers>=2.7.0  # Embedding models (SPECTER2 / NOMIC / MedEmbed)
peft>=0.10.0
einops>=0.7.0
adapters>=1.0.0
rank-bm25>=0.2.2          # BM25 sparse retrieval
nltk>=3.8.0               # Tokenisation for BM25 + text chunking

# ══════════════════════════════════════════════════════
# RESEARCH ENGINE — research_engine.py
# ══════════════════════════════════════════════════════

scipy>=1.11.0             # Meta-analysis statistics (effect sizes, heterogeneity)
matplotlib>=3.8.0         # Forest plots, funnel plots, figure generation
python-docx>=1.0.0        # .docx manuscript export
retrying>=1.3.4           # Retry decorator — scihub.py download retries

# ══════════════════════════════════════════════════════
# AUDIO — shazam_recognize.py
# ══════════════════════════════════════════════════════

pydub>=0.25.1             # Audio file loading / format conversion
mutagen>=1.47.0           # Audio metadata reading/writing (ID3, MP4, Vorbis, ASF)

# ══════════════════════════════════════════════════════
# FILE PROCESSING — process_files.py
# ══════════════════════════════════════════════════════

qrcode[pil]>=7.4          # QR code generation from URLs / text
pyminizip>=0.2.6          # Encrypted ZIP (AES-256) — fallback when 7-Zip not available

# ══════════════════════════════════════════════════════
# GEMINI EMBEDDING — RAG_engine.py (4th embedding model)
# ══════════════════════════════════════════════════════

google-genai>=0.8.0        # Gemini API client — text-embedding-004, batch file API
                           # Requires GEMINI_API_KEY env var or settings.gemini_api_key

# ══════════════════════════════════════════════════════
# GLM-OCR (TIER 1 — DISABLED)
# Set _GLM_OCR_ENABLED = True in pdf_parser.py to activate.
# GPU strongly recommended; ~44 min/page on CPU.
# Uncomment to install:
# ══════════════════════════════════════════════════════

# torch>=2.0.0
# transformers>=5.3.0
# accelerate>=0.26.0
