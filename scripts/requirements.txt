# Zenith — Python sidecar dependencies
# Install: pip install -r scripts/requirements.txt

# ── Core (required) ──
Pillow>=10.0.0            # Image processing (compress, resize, EXIF strip, WebP, palette)
qrcode[pil]>=7.4          # QR code generation from URLs/text
beautifulsoup4
retrying
# ── PDF Processing ──
pdfplumber>=0.10.0         # PDF text extraction (primary)
PyPDF2>=3.0.0              # PDF merge/compress (fallback)
pikepdf>=8.0.0             # PDF compression (preferred, faster)
reportlab>=4.0.0           # PDF generation (OCR → searchable PDF)

# ── OCR ──
pytesseract>=0.3.10        # Tesseract OCR Python wrapper (requires Tesseract binary)

# ── Audio Recognition & Metadata ──
requests>=2.31.0           # HTTP client (TheAudioDB API, cover art download)
mutagen>=1.47.0            # Audio metadata reading/writing (ID3, Vorbis, MP4, ASF)

# ── Image Processing (advanced) ──
numpy>=1.24.0              # Chroma-key background removal (array ops)

# ── Optional (enhanced features) ──
pyminizip>=0.2.6           # Encrypted ZIP (AES-256) — fallback if 7z not available

# ── Research Engine (Zenith Research Window) ──
# Note: research_engine.py uses only stdlib (urllib, xml, json, re, subprocess)
# for core functionality. The following are optional for enhanced features:
# pdfplumber (already listed above) — PDF text extraction for research
# reportlab (already listed above) — PDF export for research conversations
